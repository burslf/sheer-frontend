# this is an auto generated file. This will be overwritten
query GetUserById($clientId: ID!) {
  getUserById(clientId: $clientId) {
    client_id
    email
    first_name
    last_name
  }
}
query GetSpotifyAuth($redirectUri: String!) {
  getSpotifyAuth(redirectUri: $redirectUri) {
    auth_endpoint
  }
}
query GetLyrics($artist: String!, $title: String!) {
  getLyrics(artist: $artist, title: $title) {
    lyrics
  }
}
