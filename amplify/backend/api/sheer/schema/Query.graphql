type Query {
  getUserById(clientId: ID!): User
    @function(name: "SheerAPIoperations-${env}")
  getUserByEmail(email: String!): User
    @function(name: "SheerAPIoperations-${env}")
  getSpotifyAuth(redirectUri: String!): AuthEndpointResponse
    @function(name: "SheerAPIoperations-${env}")
  getLyrics(artist: String!, title: String!): LyricsResponse
    @function(name: "SheerAPIoperations-${env}")
  
}